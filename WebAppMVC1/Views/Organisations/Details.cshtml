@model OrganisationDetailsViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Organisation</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Organisation.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Organisation.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Organisation.FullName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Organisation.FullName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Organisation.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
<div>
    <h4>List of users</h4>
    @await Html.PartialAsync("_OrganisationUsers", Model.Users)
</div>
